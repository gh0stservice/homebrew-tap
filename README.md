# gh0stservice Homebrew Tap

Homebrew formulae for gh0stservice command-line tools.

```bash
brew tap gh0stservice/tap
brew install gh0stservice/tap/gh0stcli
```

The `gh0stcli` formula is updated by the `gh0stcli` GitLab release pipeline from signed S3 release artifacts.
